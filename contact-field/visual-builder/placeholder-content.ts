import { type ContactFieldAttrs } from '@divi/types';

/**
 * Placeholder content for a newly added Contact Field module.
 *
 * @since ??
 *
 * @type {object}
 * @property {object} fieldItem
 */
export const placeholderContent: ContactFieldAttrs = {
  fieldItem: {
    advanced: {
      fullwidth: {
        desktop: {
          value: 'on',
        },
      },
    },
  },
};
